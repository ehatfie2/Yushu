<%= form_with model: @adventure, local: true do |form| %>

    <% if @adventure.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@adventure.errors.count, "error") %> prohibited
        this adventure from being saved:
      </h2>
      <ul>
        <% @adventure.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= form.label :name %><br>
    <%= form.text_field :name %>
  </p>
  
  <p>Choose a storyline for your adventure</p>
        <p>Storyline 1<%= radio_button_tag(:storyline, "Storyline 1") %></p>
        <p>Storyline 2<%= radio_button_tag(:storyline, "Storyline 2") %></p>
  </p>

  <p>
    <%= form.submit %>
  </p>
<% end %>